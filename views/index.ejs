<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Keyword Search</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
        }
        .search-input {
            transition: box-shadow 0.3s ease;
        }
        .search-input:hover,
        .search-input:focus {
            box-shadow: 0 0 0 3px rgba(66, 133, 244, 0.2);
        }
    </style>
</head>
<body class="bg-white flex items-center justify-center min-h-screen">
    <div class="text-center w-full max-w-xl px-4">
        <div class="mb-12">
            <h1 class="text-6xl font-light text-gray-700 mb-8">
                <span class="text-blue-500">K</span>
                <span class="text-red-500">e</span>
                <span class="text-yellow-500">y</span>
                <span class="text-blue-500">w</span>
                <span class="text-green-500">o</span>
                <span class="text-red-500">r</span>
                <span class="text-blue-500">d</span>
                <span class="text-green-500"> Search</span>
            </h1>
        </div>
        <form action="/search" method="POST" class="relative">
            <div class="flex items-center border border-gray-200 rounded-full px-4 py-2 shadow-sm search-input">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-gray-400 mr-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                </svg>
                <input 
                    type="text" 
                    id="keyword" 
                    name="keyword" 
                    required 
                    placeholder="Enter your keyword"
                    class="w-full focus:outline-none text-gray-700 text-lg"
                >
            </div>
            <div class="mt-6 space-x-4">
                <button 
                    type="submit" 
                    class="bg-blue-500 text-white px-6 py-2 rounded-full hover:bg-blue-600 transition duration-300 ease-in-out focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-50"
                >
                    Search
                </button>
                <button 
                    type="button" 
                    class="bg-gray-100 text-gray-700 px-6 py-2 rounded-full hover:bg-gray-200 transition duration-300 ease-in-out focus:outline-none focus:ring-2 focus:ring-gray-300 focus:ring-opacity-50"
                >
                    I'm Feeling Lucky
                </button>
            </div>
        </form>
    </div>
</body>
</html> -->
<!-- <!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Smart Keyword Search</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@300;400;500&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Roboto', sans-serif;
        }
        .search-input {
            transition: box-shadow 0.3s ease, transform 0.2s ease;
        }
        .search-input:hover,
        .search-input:focus-within {
            box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.2);
            transform: scale(1.02);
        }
        .hero-title span {
            display: inline-block;
            transition: transform 0.3s ease, color 0.3s ease;
        }
        .hero-title span:hover {
            transform: translateY(-5px);
            color: #3b82f6;
        }
    </style>
</head>
<body class="bg-gradient-to-br from-gray-50 via-white to-gray-100 flex items-center justify-center min-h-screen">
    <div class="text-center w-full max-w-2xl px-6 py-12">
        <div class="mb-12">
            <h1 class="hero-title text-5xl md:text-6xl font-bold text-gray-800 tracking-tight">
                <span class="text-blue-500">Find</span>
                <span class="text-red-500">Everything</span>
                <span class="text-yellow-500">You</span>
                <span class="text-green-500">Need</span>
                <br>
                <span class="text-gray-700">With Ease</span>
            </h1>
            <p class="mt-4 text-lg text-gray-600">Enter a keyword to explore a world of information tailored just for you.</p>
        </div>
        <form action="/search" method="POST" class="relative">
            <div class="flex items-center border border-gray-300 rounded-full px-4 py-3 bg-white shadow-md search-input">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-6 w-6 text-gray-400 mr-3" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                </svg>
                <input 
                    type="text" 
                    id="keyword" 
                    name="keyword" 
                    required 
                    placeholder="Search for anything..."
                    class="w-full focus:outline-none text-gray-800 text-lg"
                >
            </div>
            <div class="mt-6 flex justify-center gap-4">
                <button 
                    type="submit" 
                    class="bg-blue-500 text-white px-8 py-3 rounded-full font-medium shadow hover:bg-blue-600 transition duration-300 focus:outline-none focus:ring-2 focus:ring-blue-500 focus:ring-opacity-50"
                >
                    Search Now
                </button>
                <button 
                    type="button" 
                    class="bg-gray-200 text-gray-700 px-8 py-3 rounded-full font-medium shadow hover:bg-gray-300 transition duration-300 focus:outline-none focus:ring-2 focus:ring-gray-400 focus:ring-opacity-50"
                >
                    Surprise Me!
                </button>
            </div>
        </form>
    </div>
</body>
</html> -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Smart Keyword Search</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&family=Inter:wght@300;400;500;600&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Inter', sans-serif;
            background: linear-gradient(135deg, #f5f7fa 0%, #c3cfe2 100%);
            background-attachment: fixed;
        }
        .hero-container {
            backdrop-filter: blur(10px);
            background-color: rgba(255, 255, 255, 0.7);
            border-radius: 2rem;
            box-shadow: 0 25px 50px -12px rgba(0, 0, 0, 0.1), 0 10px 15px -3px rgba(0, 0, 0, 0.05);
            padding: 3rem;
            transition: all 0.3s ease;
        }
        .hero-container:hover {
            transform: translateY(-10px);
            box-shadow: 0 35px 60px -15px rgba(0, 0, 0, 0.15);
        }
        .hero-title {
            font-family: 'Poppins', sans-serif;
            text-shadow: 1px 1px 2px rgba(0,0,0,0.05);
        }
        /* Keyframe animations for each word */
        @keyframes float1 {
            0%, 100% { transform: translateY(0) rotate(0deg); }
            25% { transform: translateY(-10px) rotate(-3deg); }
            75% { transform: translateY(10px) rotate(3deg); }
        }
        @keyframes float2 {
            0%, 100% { transform: translateY(0) rotate(0deg); }
            25% { transform: translateY(15px) rotate(4deg); }
            75% { transform: translateY(-15px) rotate(-4deg); }
        }
        @keyframes float3 {
            0%, 100% { transform: translateY(0) rotate(0deg); }
            25% { transform: translateY(-15px) rotate(2deg); }
            75% { transform: translateY(15px) rotate(-2deg); }
        }
        @keyframes float4 {
            0%, 100% { transform: translateY(0) rotate(0deg); }
            25% { transform: translateY(10px) rotate(-3deg); }
            75% { transform: translateY(-10px) rotate(3deg); }
        }
        /* Apply animations to each word */
        .hero-title span:nth-child(1) {
            animation: float1 4s ease-in-out infinite;
        }
        .hero-title span:nth-child(2) {
            animation: float2 4.5s ease-in-out infinite;
        }
        .hero-title span:nth-child(3) {
            animation: float3 5s ease-in-out infinite;
        }
        .hero-title span:nth-child(4) {
            animation: float4 4.7s ease-in-out infinite;
        }
        .hero-title span {
            display: inline-block;
            transition: all 0.4s cubic-bezier(0.175, 0.885, 0.32, 1.275);
        }
        .hero-title span:hover {
            transform: scale(1.1) rotate(5deg);
        }
        .search-input {
            transition: all 0.3s ease;
            background-color: rgba(255, 255, 255, 0.9);
        }
        .search-input:focus-within {
            box-shadow: 0 0 0 4px rgba(59, 130, 246, 0.2);
            transform: scale(1.02);
        }
        .btn-primary {
            background: linear-gradient(to right, #3b82f6, #2563eb);
            transition: all 0.4s ease;
            position: relative;
            overflow: hidden;
        }
        .btn-primary:hover {
            transform: translateY(-3px);
            box-shadow: 0 10px 20px rgba(59, 130, 246, 0.3);
        }
        .btn-primary::before {
            content: '';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            background: linear-gradient(120deg, transparent, rgba(255,255,255,0.3), transparent);
            transition: all 0.4s ease;
        }
        .btn-primary:hover::before {
            left: 100%;
        }
        .btn-secondary {
            transition: all 0.3s ease;
            background-color: rgba(229, 231, 235, 0.8);
        }
        .btn-secondary:hover {
            background-color: rgba(209, 213, 219, 0.9);
            transform: translateY(-2px);
        }
    </style>
</head>
<body class="flex items-center justify-center min-h-screen p-4">
    <div class="hero-container w-full max-w-2xl text-center">
        <div class="mb-12">
            <h1 class="hero-title text-5xl md:text-6xl font-bold text-gray-800 tracking-tight leading-tight">
                <span class="text-blue-500">Discover</span>
                <span class="text-red-500">Smart</span>
                <span class="text-yellow-500">Dynamic</span>
                <span class="text-green-500">Search</span>
                <br>
                <span class="text-gray-700">Powered by Intelligence</span>
            </h1>
            <p class="mt-6 text-xl text-gray-600 font-light">Unlock a world of information with intelligent, precision-driven search.</p>
        </div>
        <form action="/search" method="POST" class="relative">
            <div class="flex items-center border border-gray-300 rounded-full px-5 py-4 search-input">
                <svg xmlns="http://www.w3.org/2000/svg" class="h-7 w-7 text-gray-400 mr-4" fill="none" viewBox="0 0 24 24" stroke="currentColor">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M21 21l-6-6m2-5a7 7 0 11-14 0 7 7 0 0114 0z" />
                </svg>
                <input 
                    type="text" 
                    id="keyword" 
                    name="keyword" 
                    required 
                    placeholder="Explore your curiosity..."
                    class="w-full focus:outline-none text-gray-800 text-xl font-light tracking-wide"
                >
            </div>
            <div class="mt-8 flex justify-center gap-5">
                <button 
                    type="submit" 
                    class="btn-primary text-white px-10 py-4 rounded-full font-semibold shadow-lg text-lg"
                >
                    Search Intelligently
                </button>
                <!-- <button 
                    type="button" 
                    class="btn-secondary text-gray-700 px-10 py-4 rounded-full font-semibold shadow-md text-lg"
                >
                    Feeling Lucky
                </button> -->
            </div>
        </form>
    </div>
</body>
</html>